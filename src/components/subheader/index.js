import React from 'react'
import './styles.css'

const SubHeader = ({ title }) => {
    return (
        <div className="subHeader">
            <h3>{title}</h3>
        </div>

    )
}

export default SubHeader